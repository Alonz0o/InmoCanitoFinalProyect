<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inmocanito">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.propiedades.PropiedadesDetallesActivity"
            android:label="@string/detalle_propiedades"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.inquilinos.InquilinosDetallesActivity"
            android:label="@string/detalle_inquilinos"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.pagos.PagosDetallesActivity"
            android:label="@string/detalle_pagos"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.contratos.ContratosDetallesActivity"
            android:label="@string/detalle_contratos"
            android:launchMode="singleInstance" />
    </application>

</manifest>